# Auth Feature

Reusable mobile authentication foundation.

Current responsibilities:

- native Google and Apple sign-in adapters
- backend auth callback completion
- secure session persistence
- session bootstrap and refresh
- public/private shell transitions

This module is part of the starter base and should remain app-agnostic.
