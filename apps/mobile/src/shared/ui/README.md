# Shared UI

Reusable presentation-only design system and shell components.

Goals:

- keep shell UI generic
- avoid coupling UI primitives to product features
- make new app ideas faster to build without redoing the basics

This is the preferred place for primitives, composables, navigation chrome, and theme tokens.
