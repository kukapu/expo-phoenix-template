# Shared Modules

Cross-feature mobile infrastructure for the reusable starter.

Keep this area generic.

- `api` — HTTP client helpers
- `config` — runtime config and feature flag access
- `ui` — design system primitives and shell UI
- `storage` — reusable persistence helpers

Do not place app-specific business logic here.
